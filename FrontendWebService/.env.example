# Base URL of the Backend API Service
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000

# Development-only bearer token to call backend (in real deployments, use proper auth/session)
NEXT_PUBLIC_DEV_BEARER=

# Optionally, you can store an access token in localStorage at runtime under 'access_token'.
